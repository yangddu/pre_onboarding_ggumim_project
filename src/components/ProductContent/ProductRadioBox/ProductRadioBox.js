import React, { useEffect, useRef } from 'react';
import styled from 'styled-components';

function ProductRadioBox({ radioBoxRef, product }) {
  // console.log(product);
  // useEffect(() => {
  //   radioBoxRef.current.style.top = `${product.pointY}%`;
  //   radioBoxRef.current.style.left = `${product.pointX}%`;
  // }, []);
  return (
    // <Div ref={radioBoxRef}>
    //   ({product.pointX},{product.pointY})
    // </Div>
    <div></div>
  );
}

export default ProductRadioBox;

// const Div = styled.div`
//   width: 10px;
//   height: 10px;
//   background: yellow;
//   position: absolute;
// `;
